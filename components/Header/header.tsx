import styles from './header.module.css';
import { Card, Heading, Text } from 'degen';

function Header() {
    return (
        <header className={styles.header}>
            <Heading>TBC</Heading>
        </header>
    )
}

export default Header;